# PRD：do you eat（今天吃啥）

> 版本：v1.0（MVP）
> 日期：2026-09-19
> 状态：已定稿，待 Day 5 技术选型
> 前置文档：research.md（竞品调研报告，2026-09-18）

---

## 1. 产品概述

**do you eat** 是一个帮大学生解决"今天吃什么"选择困难的网页小工具。

- **一句话定位**：打开即用的"今天吃啥"抽卡决策器——点一下，抽一张，别纠结了，干饭。
- **产品形态**：纯前端静态网页，免登录、免安装，浏览器打开就能用。
- **核心价值**：用随机性终结选择困难，用抽卡的形式把"被迫做决定"变成"开盲盒的小惊喜"。
- **对应调研结论**：市面上没有专为大学生校园场景做的轻量决策工具（详见 research.md 第二节），这是本产品的机会。

## 2. 目标用户与使用场景

**目标用户**：在校大学生（住校，主要在食堂 / 校门口 / 后街吃饭）。

**典型场景**：

| 场景 | 时刻 | 用户心理 |
|---|---|---|
| 午饭纠结 | 上午下课，11:50 走出教学楼 | "食堂三层楼几十个窗口，懒得想了" |
| 晚饭纠结 | 傍晚下完课 / 图书馆出来 | "昨天吃过麻辣香锅了，今天吃啥" |
| 夜宵冲动 | 晚上 10 点宿舍里 | "想吃点啥又不知道吃啥，来一发" |

**核心使用动线**：30 秒内完成「打开页面 → 按一下 → 得到结果」。任何超出这个长度的流程都是失败。

## 3. 本期功能范围（MVP）

> 写法约定：每个功能给出「用户动作」（用户做什么）和「可观察结果」（用户能亲眼看到什么变化）。

### F1 食物随机抽卡系统（核心）

**做什么**：用户点一下启动键，随机抽出一种食物，用动画揭晓。

- **用户动作**：在首页点击「抽卡」启动键。
- **可观察结果**：屏幕出现抽卡动画（卡片翻转 / 骰子滚动等视觉过程，时长 1~3 秒），动画结束后展示抽中的食物：配图、名称、介绍文字、评分。
- **用户动作**：对结果不满意时点击「不行，重来」。
- **可观察结果**：动画重新播放，展示新的随机结果（可能与上次不同）。

**规则**：

1. 抽卡完全随机，不做任何个性化推荐（调研结论：纯随机 + 重抽已覆盖 80% 需求）。
2. 重抽次数不设上限。
3. 数据全部来自内置食物库，用户无需录入任何选项（避开转盘类 App "冷启动要自己填"的坑）。

### F2 食物评级展示

**做什么**：抽中结果和食物信息中，展示该食物的评分。

- **用户动作**：查看抽卡结果。
- **可观察结果**：结果卡片上能看到该食物的评分（星级或分数形式，如 ★4.5），以及一段介绍文字（一句话的俏皮介绍，如"后街排队之王，微辣带甜"）。
- **评分含义**：内置评分反映该食物在校园场景下的受欢迎程度，是用户"要不要相信这次抽卡结果"的参考。

### F3 用户打分（写入功能）

**做什么**：用户可以对抽中的食物打分，评分保存在本地浏览器里。

- **用户动作**：在结果卡片上点选自己的评分（如点 1~5 星中的某一星）。
- **可观察结果**：打分后，该食物的评分展示区更新为用户自己的评分（并提示"已记录你的评分"）；刷新页面或下次抽到同一食物时，自己的评分依然保留。
- **用户动作**：对同一食物重新打分。
- **可观察结果**：评分更新为最新一次的打分。

**规则**：

1. 用户评分保存在浏览器 localStorage，不上传任何服务器（本期无后端）。
2. 用户自己的评分优先于内置评分展示（自己的判断最重要）。
3. 不做评分统计、平均分计算等衍生功能（后续版本再考虑）。

## 4. 页面与交互

**页面结构**：单页应用，一个页面，两个状态。

### 状态一：首页（待抽卡）

| 元素 | 说明 |
|---|---|
| 产品名 | "do you eat" 标识 |
| 抽卡启动键 | 醒目的大按钮，文案如「今天吃啥？抽！」 |
| 引导文案 | 一句话告诉用户这是干嘛的 |

### 状态二：抽卡 / 结果

| 元素 | 说明 |
|---|---|
| 抽卡动画 | 点击启动键后播放，1~3 秒的翻转 / 滚动动画 |
| 食物配图 | 抽中食物的视觉图像（大而醒目） |
| 食物名称 | 如「麻辣香锅」 |
| 介绍文字 | 一句话俏皮介绍 |
| 评分展示 | 星级 / 分数 + 用户的打分入口（见 F2 / F3） |
| 重抽键 | 「不行，重来」 |

**交互流程**：首页点启动键 → 播放抽卡动画 → 展示结果（配图 + 名称 + 介绍 + 评分 + 打分区）→ 满意就看结果去干饭，不满意点重抽。

## 5. 数据字段定义

**内置食物库**（每条食物记录包含以下字段）：

| 字段 | 类型 | 说明 | 示例 |
|---|---|---|---|
| `id` | 唯一编号 | 每条食物的唯一标识 | `f001` |
| `name` | 名称 | 食物名 | 麻辣香锅 |
| `image` | 配图 | 该食物的视觉图像（具体形式——emoji / 本地图片 / 图床链接——由 Day 5 技术选型确定，PRD 只要求：有配图、且配图缺失时页面不报错） | （见 Day 5） |
| `rating` | 评分 | 内置评分，本期内为固定值（如 3.0~5.0 的数值） | 4.5 |
| `description` | 介绍文字 | 一句话俏皮介绍 | "后街排队之王，微辣带甜" |

**用户评分数据**（保存在 localStorage）：

| 字段 | 类型 | 说明 |
|---|---|---|
| `foodId` | 关联 id | 对应内置食物的 `id` |
| `userRating` | 用户评分 | 用户最近一次对该食物的打分 |

**数据量要求**：内置食物库不少于 20 条（覆盖食堂主菜、小吃、饮品、夜宵），保证随机结果有足够丰富性。后续可扩充到调研报告建议的 80~150 条。

## 6. 明确不做（排除项）

> 砍功能的标准：砍掉它之后，「大学生随机决定吃什么」这个核心还成立吗？成立的，砍。

### 砍到后续版本（做完 MVP 再说）

| 功能 | 理由 |
|---|---|
| **外卖平台 / 食堂数据对接**（任务清单明确要求） | 依赖外部数据和接口，零基础 28 天内做不完也养不起；核心是"决定吃什么"，不是"去哪买" |
| 时段筛选（早 / 午 / 晚 / 夜宵） | 核心是"随机出结果"，时段只是让推荐更准，不在也能用 |
| 忌口黑名单开关 | 纯随机 + 无限重抽能覆盖大部分需求 |
| 结果卡片一键复制分享 | 好功能但非核心，第一版先保证抽卡体验完整 |
| 用户自定义增删食物 | 非必需，先验证核心假设 |
| 评分统计 / 平均分 / 评分排行 | F3 只做"记录我的评分"，衍生分析后续再说 |

### 本项目永不做（沿用调研报告第六节结论）

| 功能 | 理由 |
|---|---|
| 用户账号系统 / 注册登录 | 免登录，localStorage 够用 |
| 后端服务器与数据库 | 静态网页架构，成本为零 |
| 真实餐厅 / 地图 POI / 营业状态 | 依赖高德 / 百度 API 与企业数据 |
| 外卖跳转 / 下单 / 支付 | 涉及商家合作与合规 |
| AI 个性化推荐 | 纯随机 + 重抽已覆盖 80% 需求 |
| 多人投票 / 社交社区 | 开发与运营成本高 |
| 菜谱做法 / 教学内容 | 那是下厨房的赛道 |
| 微信小程序原生版 | 网页版先验证需求 |
| 用餐记录 / 营养分析 / 卡路里 | 锦上添花，留待后续 |

## 7. 验收标准（可逐条检查）

> 使用方式：开发完成后逐条打勾，任何一条不满足即不算完成 MVP。

**功能验收**

- [ ] A1 打开页面，无需注册登录，直接看到"抽卡"启动键
- [ ] A2 点击启动键后，出现 1~3 秒的抽卡动画，期间不能重复点击启动
- [ ] A3 动画结束后展示抽中食物的：配图、名称、介绍文字、评分，四者齐全
- [ ] A4 连续抽卡 10 次，出现至少 2 种不同结果（随机性有效）
- [ ] A5 结果页有"不行，重来"按钮，点击后重新播放动画并出新结果
- [ ] A6 用户可对抽中的食物打分（点选星级 / 分数）
- [ ] A7 打分后刷新页面，再次抽到同一食物时，显示的是用户自己的评分
- [ ] A8 对同一食物重复打分，显示最新一次的打分

**数据验收**

- [ ] B1 内置食物库不少于 20 条，每条含 id / name / image / rating / description 五个字段
- [ ] B2 某条食物的配图缺失时，页面不报错、不白屏，正常展示其余信息

**边界验收**

- [ ] C1 页面中不存在任何外卖平台跳转、食堂数据读取的入口或代码
- [ ] C2 手机浏览器打开页面可正常完成一次完整抽卡流程（打开 → 抽 → 出结果）
- [ ] C3 不输入任何信息（不登录、不填选项）也能完成完整流程

---

> 附：本 PRD 对应 research.md 调研报告；技术实现方案（框架 / 配图形式 / 数据文件格式）由 Day 5 技术选型确定，不在本文档范围内。
